#include "sub_template.h"

//下面文件调用的是弃用的模板 先注释了
int main(){
	// std::cout<<"************************************dac2sycl bindIndexInit CodeGen test************************************\n\n";
    
    // RegularSlice s1 = RegularSlice("s1", 2, 2);
    // s1.SetSplitSize(2);
    // RegularSlice s2 = RegularSlice("s2", 2, 2);
    // s2.SetSplitSize(2);
    // RegularSlice s3 = RegularSlice("s3", 2, 2);
    // s3.SetSplitSize(2);
    // RegularSlice s4 = RegularSlice("s4", 2, 2);
    // s4.SetSplitSize(2);
    // RegularSlice s5 = RegularSlice("s5", 2, 2);
    // s5.SetSplitSize(2);
    // RegularSlice s6 = RegularSlice("s6", 2, 2);
    // s6.SetSplitSize(2);
	
	// Dac_Ops ops;
	// ops.push_back(s1);
	// ops.push_back(s2);
    // ops.push_back(s3);
    // ops.push_back(s4);
    // ops.push_back(s5);
    // ops.push_back(s6);

    // std::vector<std::string> sets = {"id1", "id2", "id3", "id1","id2","id3"};
    // std::vector<std::string> offsets = {"0", "0", "0", "0","0","0"};

	// std::string IndexInit = CodeGen_IndexInit(ops,sets,offsets);

	// std::cout<<IndexInit;
	// return 0;

}